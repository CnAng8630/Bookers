<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>
  <p>本のタイトル</p>
  <%= f.text_field :title %>

  <p>感想</p>
  <%= f.text_area :body %>

  <%= f.submit '保存' %>
<% end %>

<%= link_to "Show", book_path(@book.id) %>

<%= link_to "Back", "/books" %>